진도: 196~232(예제 replace까지)

JDK 1.8버전 이상 사용해야 함

public이 붙는 클래스는 자바 파일 안에 하나만 존재.

앞에가 없는 것은 default

boolean은 참,거짓값만 받음

어느 시간 동안 안쓰면 쓰레기라 인식하고 자동 삭제. 가베지 콜레팅

new라고 선언하지 않아도 =표시를 해도 동일한 거라고 선언 가능. 단점은 가베지가 될수도 있음.

null

메소드

void: return은 안해도 될시에 사용. 반환형이 없을 시 사용

this: 현재 클래스에 있는 것이라는 것을 의미.

modifier

private는 필드에 바로 접근 불가. get 메소드 사용해야지 접근 가능

반환형: int, float, double..

메소드는 제일 앞 자는 소문자로 생성----------권고사항. 필수는 아님

반환형이 있을 때 return은 필 수

중간중간 실습 잘하는지 체크하는거 10점~20점 사이 점수 들어감(4~5번 정도 검사 예정)

@Override //상속받은 메소드를 고칠 수 있는 것

오버로딩은 하나의 클래스 안에 있는 것

public String toString() {
		return "Television [채널=" +channel + ", 볼륨=" + volume + " 입니다.]";
	}

오버라이딩

문자열은 자바에서 기초 자료형이 아니다